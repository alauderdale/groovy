.container.double-padding-top
	.row
		.span12
			%h1
				New Klip

			= form_for(:shot, :url => {:action => 'create'}, :html => { :multipart => true }) do |f| 
				= render(:partial => 'form', :locals => {:f => f})
				.form-buttons
					= submit_tag("Create", :class=>"btn btn-primary") 