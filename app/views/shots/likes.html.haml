= render :partial => 'shared/breadcrumb', :locals => {:item => @shot, :item_title =>@shot.title, :title => @title}

-@shot.likes.each do |v|
	= render :partial => 'shared/user_list', :locals => {:user => v.voter}